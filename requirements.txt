ultralytics>=8.4.0
# ultralytics' ByteTrack/BoT-SORT trackers need this for their assignment
# algorithm but don't install it automatically -- without it, any
# model.track(...) call (both here and in tomato_harvest_system.py) fails
# with "ModuleNotFoundError: No module named 'lap'". lapx (not the original
# lap package) is what ultralytics' own current source recommends: broader
# prebuilt-wheel coverage across platforms/Python versions, and it provides
# the same `import lap` interface lap itself does.
lapx>=0.5.2
opencv-python>=4.8.0
requests>=2.31.0
streamlit>=1.57.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pandas>=2.0.0
pytest>=8.0.0
httpx>=0.27.0
